# A5b-preflight — report

Program `ai-final-review` · Control plane `PhysShell/review-governance` ·
Branch `experiment/a5b-preflight`, based on the frozen A5a evidence at
`9ea7055212d5…`. Date: 2026-08-26 (UTC).
Preregistered protocol: `experiments/preflight/PROTOCOL.md`.

PR #12 is frozen and was not modified.

## Result

```text
A5b_PREFLIGHT: PASS
```

Credentials, runtime and monitoring are in their production configuration.
No GitHub enforcement state was touched: `ai/final-review` has zero runs on
both open PR heads, the repository has zero rulesets, and `main` is
unprotected at `047ff1a641e3…`.

## 1 — App key separation

Both keys were generated by the owner in the App settings UI, because there
is no REST endpoint for it. Each was then proven independently, from the
host that would use it, against readbacks rather than assumptions.

```text
K_primary  81e1860652de83dd  primary   6/6 PASS
K_edge     c7fe54edbae7929e  edge      6/6 PASS, proven from the edge itself
K0         5f559f7e1f30eb0b  the shared key being retired
```

Each proof covers: private key readable · JWT accepted · `app.id == 4669438`
· installation `155393018` present · installation token minted · expected
repository accessible. Nothing is inferred from a previous step; a minted
token does not prove the repository is reachable, so both are checked.

`K_edge` was delivered over stdin and never existed as a separate file in a
readable location on the way. The downloaded copies were removed from the
Windows Downloads folder after installation — which reduces further
exposure without undoing the fact that the material passed through that
filesystem.

### A defect the rotation itself exposed

`openssl` is absent from `PATH` outside the flake devshell. It produced zero
bytes, and `sha256("")` is a perfectly stable value, so all three distinct
keys reported the same fingerprint:

```text
app        e3b0c44298fc1c14
K_primary  e3b0c44298fc1c14
K_edge     e3b0c44298fc1c14
```

A rotation cannot be verified by a constant. `fingerprint()` now refuses to
hash empty output and fails the whole verdict when it cannot derive a public
key.

### Revocation, proven from both hosts

The owner deleted `K0` in the App settings. Afterwards:

```text
K0         5f559f7e1f30eb0b   REJECTED   HTTP 401
                              "A JSON web token could not be decoded"
                              observed from the primary AND from the edge
K_primary  81e1860652de83dd   PASS 6/6   from the primary
K_edge     c7fe54edbae7929e   PASS 6/6   from the edge itself
```

Residual material removed, which turned out to be more than the obvious
file:

```text
primary  app.pem                    deleted
primary  app-credentials.json       `pem` field emptied — it held a second
                                    copy of K0; client_secret preserved
primary  K_edge.pem                 deleted — it was the delivery copy, and
                                    two keys on one host is not a split
primary  user-token.json            deleted — a live token pair from
                                    generation 0, read by nothing
primary  webhook-secret.retired-a2a deleted
edge     app.pem                    deleted
```

`K_edge.pem` on the primary is worth naming separately: while both keys sit
on the same host, the rotation isolates nothing, because one compromise
yields both halves.

### What this is, restated

Rotation isolation, not permission isolation. Both keys carry the authority
of the same App, so an attacker holding the edge PEM before revocation holds
the App's repository permissions and bypasses `WatchdogCapability` entirely
— that class is a property of the edge *code*, not of the credential. What
is bought is precise: the edge credential can be killed without killing the
primary.

## 2 — Alerting

Channel: Telegram (`@review_governance_bot`) for Governor and edge
incidents; email via HetrixTools for off-host availability. Both leave the
primary and the edge.

### Payload is an allowlist

Fields outside `{severity, cause, repo, pr_number, check_run_id,
incident_id, detected_at, state}` raise rather than being stripped, and
causes must be preregistered. A redaction pass is a list of the leaks
somebody thought of; there is nowhere here to put a webhook body, an OAuth
token, provider content or a PEM.

### Alerts are transitions

A standing condition does not re-page every poll. A cleared one sends a
recovery. An undelivered alert is retried every poll, and an undelivered
*recovery* keeps the incident open — otherwise a channel outage would
silently close an incident nobody knows is still running.

### Proven end to end, twice, on a real condition

The synthetic drill passed first (`incident_delivered`,
`recovery_delivered`, condition closed). More usefully, a real outage:

```text
receiver stopped   2026-08-26T12:15:27Z
receiver started   2026-08-26T12:19:00Z
outside view       HTTP 502 via Caddy throughout

12:15:29Z  WARNING   webhook_receiver_unavailable  delivered   +2 s
12:15:39Z  CRITICAL  watchdog_not_polling          delivered  +12 s
12:19:03Z  RECOVERY  webhook_receiver_unavailable  delivered
12:19:13Z  RECOVERY  watchdog_not_polling          delivered
open afterwards: none
```

### The defect this row exists to catch

The **first** attempt delivered nothing to anyone. The monitor was
configured correctly, the contact list was bound to it, the downtime was
detected from three locations and recorded, and the uptime report was
accurate — and the recipient address carried a transposition typo
(`kcousrce` for `kcsource`), so every alert went to a stranger's mailbox.

Everything green, nobody told. That is exactly why this row demanded
delivery to a human instead of a successful `curl`, and it is the same
failure shape the whole program exists to refuse. It was found by reading
the account through the HetrixTools API rather than by another round of
screenshots; contact lists are read-only there, so the fix was the owner's,
in the UI.

### Two more defects found while wiring this

**`delivery_stuck` could never clear.** Nothing ever advanced
`processing_state` on the edge, so the warning would light once and stay
lit, which is how an alert class gets ignored. The primary now POSTs an
authenticated, monotone `/signals/ack` after its cursor advances. It is
advisory in the strict sense: the primary's cursor remains the only
authority, reconciliation ignores both, and `DROPPED` rows are never
touched so the A5a-c2 reconciliation fixture stays visible.

**The sentinel paged on its own startup.** At 10:27:38Z it raised
`reconciliation_stale` simply because it started before the first sweep
landed, and recovered 22 s later. Every restart of the stack would wake the
operator for nothing, and an operator woken by routine restarts stops
reading the alerts that matter. Fixed with a startup grace that applies to
`NOT_REPORTED` only — the *absence* of data. A timestamp that exists and is
old is a real condition and still pages immediately, at startup like any
other time.

### A compromise recorded rather than hidden

`watchdog_not_polling` and `webhook_receiver_unavailable` both fire for a
single root cause: two messages, one incident. Suppressing the first would
lose visibility of the watchdog for the entire duration of a receiver
outage, which is worse. Recorded for the owner to rule on.

## 3 — User authorization: the wire, and the transition it feeds

The gap here was not a missing dashboard tile. By A1c semantics `AUTH_LOST`
and `REFRESH_OUTCOME_UNKNOWN` forbid provider triggers, invalidate standing
successes and demand fresh qualification — so a state nobody produces is a
safety transition nobody can enter. The owner ruled it blocking, correctly.

### Two readers, deliberately different objects

```text
Governor lifecycle -> authoritative store (auth.sqlite3) -> transition
sentinel           -> auth-state.json projection         -> alerts a human
```

The projection is written one way and never read back. A test tampers with
the mirror and asserts the authority is unmoved, because a world-readable
file deciding whether the gate opens is not a design, it is an accident
waiting to be discovered.

`PERMITS_TRIGGERS` is an allowlist of exactly `{AUTHORIZED}`, so a state
nobody anticipated fails closed instead of falling through a negative check.
No observation at all is not permission: a Governor that never established
authorization has not established it.

### The controlled refresh

The stored access token had expired four days earlier — the normal
eight-hour condition, which the producer refuses to call a revocation. That
left the production composition unable to publish any success at all, which
is correct fail-closed behaviour and also meant a live `AUTHORIZED` had
never been observed. The owner authorised **exactly one** refresh attempt,
with no retry under any outcome.

```text
G3  091a70cf2f39eab6 access / cd6894a851456335 refresh
    -> one request, HTTP 200
    -> structurally valid: access_token, refresh_token, expires_in,
       refresh_token_expires_in all present
    -> G4 persisted atomically, fsync on file and directory
    -> independent readback: generation_matches, access_token_matches
    -> real authenticated call succeeded as PhysShell
    -> producer emitted AUTHORIZED
    -> sentinel observed AUTHORIZED, generation 4
```

One deviation from the literal order, stated rather than slipped in: the
contract validates before persisting, which would discard a live refresh
token whenever an expiry field is missing. The implementation persists the
moment both secrets exist and only then judges, marking the credential
`validated: false` if the response was structurally incomplete. `AUTHORIZED`
still requires full validity, the gate still closes through the auth store
rather than through the credential file, and a recoverable credential is not
destroyed to satisfy an ordering.

There is no retry anywhere in that function, deliberately. If the connection
dies at the worst millisecond, a second request either fails against a spent
token or succeeds and discards a rotation nobody recorded. That uncertainty
is what `REFRESH_OUTCOME_UNKNOWN` is for; it is not a thing to be retried
away.

### Superseded generations keep no secrets

```text
history entry: generation, label, obtained_at, obtained_via,
               access_token_fingerprint, refresh_token_fingerprint,
               superseded_at, secrets_removed
```

No token-shaped string survives in the history — asserted by regex, in a
test and again live. A retained refresh token buys an audit nothing and
gives an attacker one more candidate to try.

### The integration fixture

Isolated ref, probe context, `ai/final-review` untouched throughout.

```text
1  publish success under the production composition
       auth_state AUTHORIZED, generation 4
       check run 98188457434, observed success, CONFIRMED

2  inject REFRESH_OUTCOME_UNKNOWN into the durable store (source: fixture,
   so the audit trail says injected, not observed)
       permits_triggers        false
       demands_invalidation    true

3  trigger path refuses
       "provider triggers forbidden while user authorization is
        REFRESH_OUTCOME_UNKNOWN"

4  governor.publish success refuses, with the same exception

5  safety transition
       standing success found: 98188457434
       revoked, observed failure, projection CONFIRMED
       restores_automatically: false

6  independent GitHub readback
       conclusion failure
       "Governor: EVIDENCE_INVALIDATED (authorization)"

7  Telegram alert delivered
       13:06:30Z CRITICAL refresh_outcome_unknown

8  return to AUTHORIZED
       invalidations: []
       the revoked check run on GitHub: still failure
       13:07:10Z RECOVERY refresh_outcome_unknown delivered

9  the authoritative store was then re-observed for real, so production
   state is not left resting on a fixture:
       AUTHORIZED, source device_flow, generation 4
```

Step 9 matters more than it looks. Leaving the production auth store holding
a fixture-injected `AUTHORIZED` would have been exactly the class of quiet
fiction this programme keeps finding.

## 4 — Production processes, running

```text
edge     governor-edge.service       active/enabled  Restart=always
         governor-watchdog.service   active/enabled  Restart=always
                                     --branches main --context ai/final-review
                                     --window 0
primary  governor-heartbeat.service  active/enabled  15 s
         governor-signals.service    active/enabled  fast path, --window 0
         governor-reconcile.service  active/enabled  sweep, 30 s
         governor-sentinel.service   active/enabled  20 s
```

All six with `NRestarts=0`. The watchdog currently guards nothing, because
`ai/final-review` does not exist — which makes it the ideal subject for a
production smoke test.

### A running watchdog is not a watching watchdog

Nothing paged if the watchdog stopped. `Restart=always` covers a crash but
not a hang, `systemctl is-active` is true for a loop that stopped looping,
and the off-host monitor watches the *receiver*. The independent failure
domain could have gone quiet with every dashboard green.

The watchdog now stamps every poll into the edge database, `/healthz`
exposes `last_watchdog_poll` and a counter, and the sentinel raises
`watchdog_not_polling` past 60 s — treating an unreachable edge as
`NOT_REPORTED` rather than as fine. This is one event beyond the
preregistered minimum list, added deliberately.

### Soak, counted rather than calculated

The first summary derived poll counts by dividing the window by the
interval. Elapsed time proves a poll was *due*, not that it happened. The
observer counts distinct observed values only.

```text
window          2026-08-26T10:35:35Z .. 10:38:36Z   (79 /healthz samples)

distinct heartbeats                 12   required >= 4
distinct reconciliation sweeps       6   required >= 2
distinct watchdog poll timestamps   19   required >= 2
watchdog poll counter delta         18
```

## Runtime pinning, fixed on the way

`installs[0]` is gone from `governor.py` and `edge_watchdog.py`, replaced by
a pinned installation id that fails closed on mismatch. It is the same shape
of guess as the `pulls[0]` defect A5a-c2 removed from the fast path: correct
with one installation, silently wrong with two.

## Acceptance matrix

```text
PRIMARY_APP_KEY_SEPARATE             PASS
EDGE_APP_KEY_SEPARATE                PASS
OLD_SHARED_KEY_REVOKED               PASS
PRIMARY_AUTH_AFTER_ROTATION          PASS
EDGE_AUTH_AFTER_ROTATION             PASS

OFFHOST_HEALTH_MONITOR               PASS
DOWN_DELIVERY_TO_HUMAN               PASS (confirmed by the recipient)
UP_DELIVERY_TO_HUMAN                 PASS (confirmed by the recipient)
INCIDENT_ALERT_END_TO_END            PASS
RECOVERY_ALERT_END_TO_END            PASS

EDGE_RECEIVER_RUNNING                PASS
WATCHDOG_RUNNING_CONTINUOUSLY        PASS
HEARTBEAT_HEALTHY                    PASS
FAST_PATH_HEALTHY                    PASS
RECONCILIATION_HEALTHY               PASS

CURRENT_REAL_AUTHORIZATION           PASS
USER_AUTHORIZATION_REPORTING         PASS
AUTH_FAILURE_REACHES_GATE            PASS
AUTH_FAILURE_ALERT_DELIVERED         PASS
AUTH_RECOVERY_DOES_NOT_RESTORE       PASS

PRODUCTION_CONTEXT_STILL_UNUSED      PASS
MAIN_RULESET_STILL_ABSENT            PASS
```

Accepted for v1 by the owner and recorded rather than silently applied:

```text
duplicate-alert policy   ACCEPTED — two observable consequences of one
                         cause; correlation belongs on a presentation
                         layer, not in detection semantics
startup grace            ACCEPTED
```

The HetrixTools API token used to diagnose the recipient typo was revoked by
the owner. Deleting the local file was not the revocation and is not
recorded as one.

## Stop

The A5b cutover sequence is a separate approval and does not begin from
here. The last green preflight test does not authorise the first production
check run, and it will look especially obvious when everything is green,
which is the point.

## For the future cutover

The A5a bootstrap dry run is an illustration, not an input. PR #8 stands at
`8aeafa9c28b9…` and #12 at `e29621f54a63…`; A5b must re-enumerate open PRs
and freeze `{PR, full HEAD, draft}` immediately before bootstrap.
